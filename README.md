# Font Game - Опознавание шрифтов

Онлайн-игра по опознаванию шрифтов, разработанная на React с использованием Supabase для аутентификации и хранения данных.

## 🎮 Особенности

- **20 раундов** угадывания шрифтов с прогрессивной сложностью
- **Авторизация** через Google, Yandex, VK, GitHub
- **Сохранение результатов** и лучший счет
- **Таблица лидеров** с фильтрами по времени
- **Адаптивный дизайн** для мобильных устройств и компьютеров
- **Красивые анимации** карточек и интерфейса
- **Анализ результатов** по категориям шрифтов

## 🚀 Быстрый старт

### Локальная разработка

1. **Клонирование репозитория**
```bash
git clone <repository-url>
cd font-game
```

2. **Установка зависимостей**
```bash
npm install
```

3. **Настройка переменных окружения**
```bash
cp .env.example .env
```

Отредактируйте `.env` и добавьте свои учетные данные Supabase:
```
VITE_SUPABASE_URL=your-supabase-url
VITE_SUPABASE_ANON_KEY=your-supabase-anon-key
```

4. **Настройка Supabase**
   - Создайте проект на [supabase.com](https://supabase.com)
   - В SQL Editor выполните скрипт из файла `supabase.sql`
   - Включите OAuth-провайдеры в Authentication > Providers:
     - Google
     - Yandex
     - VK
     - GitHub
   - Скопируйте URL и anon key из Settings > API

5. **Запуск development server**
```bash
npm run dev
```

Приложение будет доступно по адресу `http://localhost:3000`

## 📦 Деплой на Cloudflare Pages

### Автоматический деплой

1. **Подключите репозиторий** к Cloudflare Pages через GitHub
2. **Настройте build settings**:
   - **Framework preset**: Vite
   - **Build command**: `npm run build`
   - **Build output directory**: `dist`
3. **Добавьте переменные окружения** в Cloudflare:
   - `VITE_SUPABASE_URL`
   - `VITE_SUPABASE_ANON_KEY`
4. **Деплой** произойдет автоматически при push в репозиторий

### Ручной деплой

```bash
# Сборка проекта
npm run build

# Деплой с Wrangler (установите сначала: npm install -g wrangler)
wrangler pages publish dist
```

## 🎯 Как играть

1. Нажмите "Начать игру"
2. Угадайте шрифт по букве на карточке
3. После выбора карточка перевернется и покажет правильный ответ
4. Нажмите "Дальше" для перехода к следующему раунду
5. В конце игры увидите свой результат и анализ по категориям
6. Поделитесь результатом с друзьями!

## 🗂️ Структура проекта

```
font-game/
├── src/
│   ├── components/
│   │   ├── Game.jsx          # Основной компонент игры
│   │   ├── FontCard.jsx      # Карточка с шрифтом
│   │   ├── GameResults.jsx   # Экран результатов
│   │   ├── Leaderboard.jsx   # Таблица лидеров
│   │   └── Login.jsx        # Компонент входа
│   ├── data/
│   │   └── fonts.json        # База данных шрифтов
│   ├── App.jsx              # Главный компонент
│   ├── main.jsx             # Точка входа
│   ├── index.css            # Глобальные стили
│   └── supabaseClient.js    # Конфигурация Supabase
├── public/                  # Статические файлы
├── index.html               # HTML шаблон
├── vite.config.js           # Конфигурация Vite
├── package.json             # Зависимости
├── supabase.sql            # SQL скрипт для базы данных
└── README.md               # Этот файл
```

## 🎨 Добавление новых шрифтов

Шрифты хранятся в `src/data/fonts.json`. Формат:

```json
{
  "id": 1,
  "name": "Font Name",
  "year": 2024,
  "author": "Author Name",
  "description": "Description of the font",
  "links": ["https://example.com"],
  "tags": {
    "type": "тип шрифта",
    "style": "стиль шрифта",
    "characteristics": ["характеристика1", "характеристика2"]
  },
  "letter": "A"
}
```

### Типы шрифтов
- **антиква** - с засечками
- **гротеск** - без засечек
- **брусковый** - с толстыми засечками

### Стили шрифтов
- **старостильная антиква**
- **трансформационная антиква**
- **дидоне**
- **неогротеск**
- **геометрический гротеск**
- **гуманистический гротеск**
- И многие другие...

## 🔧 Технологический стек

- **React 19** - UI библиотека
- **Vite** - Сборщик и dev server
- **Supabase** - База данных и аутентификация
- **CSS3** - Стили и анимации
- **Cloudflare Pages** - Хостинг

## 📝 Скрипты

```bash
npm run dev      # Запуск development server
npm run build    # Сборка для production
npm run preview  # Предпросмотр production сборки
```

## 🤝 Вклад в развитие

1. Fork репозитория
2. Создайте ветку для вашей функции (`git checkout -b feature/AmazingFeature`)
3. Commit изменения (`git commit -m 'Add some AmazingFeature'`)
4. Push в ветку (`git push origin feature/AmazingFeature`)
5. Откройте Pull Request

## 📄 Лицензия

Этот проект распространяется под лицензией ISC.

## 👨‍💻 Автор

Создано с ❤️ для любителей типографики

---

**Приятной игры! 🎮**
